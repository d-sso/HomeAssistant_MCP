# Phase 2 — Control tools (not yet implemented)
#
# This module will expose service call tools once the human confirmation
# gate is in place. See docs/architecture.md for the proposed flow.
#
# Planned tools:
#   call_service(domain, service, data)
#   turn_on(entity_id, **kwargs)
#   turn_off(entity_id)
#   toggle(entity_id)
#   set_value(entity_id, value)
#   activate_scene(scene_id)
#   run_script(script_id)
#   trigger_automation(automation_id)
#   send_notification(target, message, title)
